{
  "name": "kirakira",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "prepare": "husky",
    "dev": "npm run dev --workspace=@kirakira/web",
    "dev:api": "npm run dev --workspace=@kirakira/api",
    "verify": "npm run type-check --workspaces --if-present && npm run lint --workspace=@kirakira/web && npm run test --workspace=@kirakira/web -- --run && npm run test --workspace=@kirakira/api -- --run && npm run build --workspace=@kirakira/web && npm run build --workspace=@kirakira/api"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "overrides": {
    "three": "0.184.0"
  }
}
